---- =========================================================================================================
---- WORKING SQL QUERY FOR TX_CURR (AJUDA) DATASET PRODUCTION
---- BASED ON CDC MOZAMBIQUE RETENTION DATA TEMPLATE
---- AUTHOR: Mala (CDC/GDIT) and Timoteo (CDC-MOZ) based on original by Randy (CDC/GDIT)
---- REV DATE: 6/AUG/2019
---- Pickup: Maxes on datatarv and dataseguimento before outcome date on filtered subset USING ROWNUMBERS
---- =========================================================================================================

--******************CHANGES.....08.29.2019.....**************************
----need to change 'outcome date' to 'status evaluation date'
--need to define status evaluation date ( can we define 6 different ones?) 
---		ADD that patient need to be enrolled in program (dataabertura in t_patiente is not null) (done)
---		exclude patients who do not have Max_dataproxima
----	Don't need the condition of being active if in future because it is already included (done)
			/*-- For transfer outs ensure that if saida is after MAX_TARV (actual pick up) and after MAX_Seguimento (actual consultation) 
			--	but before evaluation date patients are considered not active (done)*/

--******************CHANGES.....09.09.2019.....**************************
----exclude people who are not in the program. They are not LTFU. So don't calculate TX_CURR Active or not for them.

-----	=================================== JUNE 2018   

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ

	 /*, person.Gravidez*/

	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-06-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath



	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-06-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

		-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-06-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-06-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath




),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_JUN18
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_JUN18
SET Evaluation_Date='2018-06-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_JUN18
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_JUN18			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_JUN18
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUN18
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUN18
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUN18
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_JUN18

Go


-----	=================================== JULY 2018   

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second 

	 	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ

			/*, person.Gravidez*/

	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-07-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-07-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

		-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-07-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-07-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_JUL18
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_JUL18
SET Evaluation_Date='2018-07-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_JUL18
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_JUL18			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_JUL18
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUL18
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUL18
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUL18
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_JUL18

Go



-----	=================================== AUGUST 2018   

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ


	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-08-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-08-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

		-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-08-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-08-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_AUG18
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_AUG18
SET Evaluation_Date='2018-08-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_AUG18
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_AUG18			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_AUG18
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_AUG18
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_AUG18
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_AUG18
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_AUG18

Go


-----	=================================== SEPTEMBER 2018   

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ

	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-09-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-09-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

		-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-09-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-09-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_SEP18
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_SEP18
SET Evaluation_Date='2018-09-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_SEP18
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_SEP18			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_SEP18
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_SEP18
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_SEP18
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_SEP18
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_SEP18

Go


-----	=================================== OCTOBER 2018   

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ

	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-10-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-10-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

		-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-10-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-10-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_OCT18
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_OCT18
SET Evaluation_Date='2018-10-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_OCT18
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_OCT18			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_OCT18
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_OCT18
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_OCT18
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_OCT18
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_OCT18

Go




-----	=================================== NOVEMBER 2018   

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ


	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-11-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-11-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

		-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-11-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-11-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath

),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_NOV18
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_NOV18
SET Evaluation_Date='2018-11-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_NOV18
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_NOV18			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_NOV18
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_NOV18
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_NOV18
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_NOV18
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_NOV18

Go


-----	=================================== DECEMBER 2018   

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ


	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-12-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-12-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

		-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-12-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2018-12-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_DEC18
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_DEC18
SET Evaluation_Date='2018-12-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_DEC18
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_DEC18			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_DEC18
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_DEC18
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_DEC18
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_DEC18
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_DEC18

Go


-----	=================================== JANUARY 2019   

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ


	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-01-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-01-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

		-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-01-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-01-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_JAN19
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_JAN19
SET Evaluation_Date='2019-01-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_JAN19
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_JAN19			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_JAN19
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JAN19
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JAN19
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JAN19
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_JAN19

Go



-----	=================================== FEBRUARY 2019 

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ



	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-02-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-02-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-02-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-02-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_FEB19
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_FEB19
SET Evaluation_Date='2019-02-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_FEB19
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_FEB19			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_FEB19
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_FEB19
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_FEB19
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_FEB19
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_FEB19

Go


-----	=================================== MARCH 2019 

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ


	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-03-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-03-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-03-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-03-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_MAR19
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_MAR19
SET Evaluation_Date='2019-03-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_MAR19
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_MAR19			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_MAR19
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_MAR19
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_MAR19
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_MAR19
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_MAR19

Go


-----	=================================== APRIL 2019 

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/

	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ



	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-04-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-04-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-04-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-04-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_APR19
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_APR19
SET Evaluation_Date='2019-04-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_APR19
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_APR19			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_APR19
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_APR19
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_APR19
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_APR19

Go


-----	=================================== MAY 2019 

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/


	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ


	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-05-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-05-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-05-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-05-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_MAY19
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_MAY19
SET Evaluation_Date='2019-05-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_MAY19
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_MAY19			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_MAY19
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_MAY19
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_MAY19
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_MAY19
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_MAY19

Go


-----	=================================== JUN 2019 

WITH CTE0 AS
(
	SELECT DISTINCT 
	facility.HdD, facility.Provincia, facility.Distrito, facility.designacao,
	person.nid, person.sexo, person.datanasc, person.idade as idadeiniciotarv, person.datainiciotarv
	, YEAR(person.datainiciotarv) as Year_Inicio, person.datadiagnostico,  
	tt.Max_datatarv, ss.Max_dataseguimento,person.dataabertura
	,person.codestado, person.datasaidatarv, tt.Evaluation_Date, tt.Max_dataproxima_tarv, DayswithARV, ss.Max_dataproximaconsult
	,datatarv_second, proximatarv_second, Second_DayswithARV
	 ,dataseguimento_second, proximaconsult_second /*, person.Gravidez*/


	 , CASE WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<1 THEN 'LT_1_Year_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=1 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<3 THEN '1_3_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>=3 AND datediff(yy,person.datainiciotarv,tt.Evaluation_Date)<5 THEN '3_5_Years_ART'
			WHEN datediff(yy,person.datainiciotarv,tt.Evaluation_Date)>5 THEN 'GT_5_Years_ART'
			END AS time_ART_categ

	FROM
	(SELECT nid, sexo, cast(datanasc as date) as datanasc, cast(dataabertura as date) as dataabertura, idade, hdd, codproveniencia, cast(datainiciotarv as date) as datainiciotarv
	, cast(datadiagnostico as date) as datadiagnostico, codestado, cast(datasaidatarv as date) as datasaidatarv, AccessFilePath
	/*,Gravidez = CASE WHEN codproveniencia = 'PTV' AND idade >= '14' Then 1 END */
	FROM t_paciente) person
	/*
	LEFT JOIN
	(SELECT nid, gravida, AccessFilePath
	FROM t_adulto) adult
	on person.nid=adult.nid AND person.AccessFilePath=adult.AccessFilePath
	*/
	LEFT JOIN
	(SELECT HdD, Provincia, Distrito, designacao, AccessFilePath
	FROM t_hdd) facility
	ON person.hdd = facility.HdD

	-- Joining subset of filtered dates from t_tarv below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as Max_datatarv, ntv.dataproxima as Max_dataproxima_tarv, ntv.dias as DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-06-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '1') tt
	ON person.nid = tt.nid AND person.AccessFilePath = tt.AccessFilePath

	-- Joining subset of filtered dates from t_seguimento below @RetentionType outcome date
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as Max_dataseguimento, nts.dataproximaconsulta as Max_dataproximaconsult
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-06-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '1') ss
	ON person.nid = ss.nid AND person.AccessFilePath = ss.AccessFilePath

-- Second to last tarv pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY ntv.nid, ntv.AccessFilePath ORDER BY ntv.datatarv desc) as rownum, ntv.nid, ntv.AccessFilePath, ntv.Evaluation_Date
	, ntv.datatarv as datatarv_second, ntv.dataproxima as proximatarv_second, ntv.dias as Second_DayswithARV
	FROM
		(
		SELECT tv.nid, tv.AccessFilePath, cast(datatarv as date) as datatarv, cast(dataproxima as date) as dataproxima, tpo.Evaluation_Date, dias
		FROM t_tarv tv
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-06-21', AccessFilePath
		FROM t_paciente) tpo
		ON tv.nid = tpo.nid AND tv.AccessFilePath = tpo.AccessFilePath
		WHERE cast(datatarv AS date) <= Evaluation_Date
		) ntv
	) t
	WHERE t.rownum = '2') t2
	ON person.nid = t2.nid AND person.AccessFilePath = t2.AccessFilePath


		-- Second to last consultation pick up joined
	LEFT JOIN
	(SELECT * FROM(
	SELECT ROW_NUMBER() OVER (PARTITION BY nts.nid, nts.AccessFilePath ORDER BY nts.dataseguimento desc) as rownum, nts.nid, nts.AccessFilePath
	, nts.dataseguimento as dataseguimento_second, nts.dataproximaconsulta as proximaconsult_second
	FROM 
		(
		SELECT ts.nid, Gravidez, ts.AccessFilePath, cast(dataseguimento as date) as dataseguimento, cast(dataproximaconsulta as date) as dataproximaconsulta
		, tpo1.Evaluation_Date
		FROM t_seguimento  ts
		LEFT JOIN
		(SELECT nid, Evaluation_Date = '2019-06-21', AccessFilePath
		FROM t_paciente) tpo1
		ON ts.nid = tpo1.nid AND ts.AccessFilePath = tpo1.AccessFilePath
		WHERE cast(dataseguimento AS date) <= Evaluation_Date
		) nts
	) s
	WHERE s.rownum = '2') s2
	ON person.nid = s2.nid AND person.AccessFilePath = s2.AccessFilePath
),

/*
===================================== Calculations for active patients ============================
====General conditions====
1. Patients must be enrolled.
2. Patients has to have next pick date (maybe consultation as well???)
3. Patients have started ART before the date when we are evaluating TX_CURR (end of period or any other date chosen)
======Calculations============
1. There is no data de saida--> Compare the dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
2. Data de saida is after Evaluation--> Compare dates to confirm patient has not left (activity within 60 days/has not missed more than 30 days of visit of pick up).
3. Data saida is before Evaluation but there was activity after saida --> Compare dates to confirm activity registered was within 60 days of Evaluation.
4. All others are not active including all who left before Evaluation and have not registered actual activity after data saida.

*/
CTE1 AS   
( 
	SELECT *, 
	CASE WHEN 
	dataabertura IS NOT NULL AND
	(Max_dataproxima_tarv IS NOT NULL AND Max_dataproximaconsult IS NOT NULL) AND
(
	(
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND
	
				((datasaidatarv IS NULL) AND
							
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
				)
				 
	)
		
	
							OR
	
	(	
		(cast(datainiciotarv AS DATE) <Evaluation_Date) AND

			((datasaidatarv IS NOT NULL) AND
			(datasaidatarv > Evaluation_Date) AND
			
				(	(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date)) OR 
					(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date)) 
				)
					
			)
	)

	
							OR 
	(	
		(cast(datainiciotarv AS DATE) < Evaluation_Date) AND

		((datasaidatarv IS NOT NULL) AND
			
					(datasaidatarv<=Evaluation_Date) AND 
					((datasaidatarv<Max_datatarv AND datasaidatarv<Max_dataseguimento)	OR
					(datasaidatarv<Max_datatarv AND Max_dataseguimento IS NULL)			OR
					(Max_datatarv IS NULL AND datasaidatarv <Max_dataseguimento))		AND
					
						(Max_dataproxima_tarv >= dateadd(dd,-30,Evaluation_Date))		OR 
						(Max_dataproximaconsult >= dateadd(dd, -30, Evaluation_Date))
			
		)
)
)
	THEN 'Active'

	Else 'Not Active'
	END AS [TX_CURR]
	FROM CTE0
),
CTE2 AS
( 
	SELECT *, CASE WHEN TX_CURR = 'Not Active' AND codestado = 'ABANDONO' AND datasaidatarv <= Evaluation_Date THEN 'Abandon'
	WHEN TX_CURR = 'Not Active' AND codestado = 'SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date THEN 'ART Suspend'
	WHEN TX_CURR = 'Not Active' AND codestado = 'TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date THEN 'Transferred Out'
	WHEN TX_CURR = 'Not Active' AND codestado = 'OBITOU' AND datasaidatarv <= Evaluation_Date THEN 'Dead'
	WHEN TX_CURR = 'Not Active' AND codestado IS NULL THEN 'LTFU'
	WHEN TX_CURR = 'Active' Then 'Active'
	ELSE NULL
	END AS [Outcome]
	FROM CTE1)


/******just insert new tx_curr based on status******/

-----rename table
SELECT *
INTO Sandbox.dbo.TX_CURR_JUN19
FROM CTE2
/*WHERE datainiciotarv >= '2012' AND  Evaluation_Date IS NOT NULL*/
ORDER BY nid, Outcome  desc

UPDATE Sandbox.dbo.TX_CURR_JUN19
SET Evaluation_Date='2019-06-21'
Where Evaluation_Date is null

UPDATE Sandbox.dbo.TX_CURR_JUN19
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv > Evaluation_Date AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_JUN19			/*****check how JEMBI handles cases without datasaida tarv****/
SET Outcome='LTFU' WHERE Outcome is null AND datasaidatarv is NULL AND 
((Max_dataproxima_tarv < dateadd(dd,-30,Evaluation_Date)) OR 
(Max_dataproximaconsult < dateadd(dd, -30, Evaluation_Date)))


Update Sandbox.dbo.TX_CURR_JUN19
SET Outcome='Abandon' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado='ABANDONO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUN19
SET Outcome='ART Suspend' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='SUSPENDER TRATAMENTO' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUN19
SET Outcome='Transferred Out' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='TRANSFERIDO PARA' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)

Update Sandbox.dbo.TX_CURR_JUN19
SET Outcome='Dead' WHERE Outcome is null AND TX_CURR ='Not Active' AND codestado ='OBITOU' AND datasaidatarv <= Evaluation_Date AND (Max_datatarv<Evaluation_Date OR Max_dataseguimento<Evaluation_Date)


SELECT *,
CASE WHEN Max_datatarv>Max_dataseguimento
	THEN DATEDIFF(dd,Evaluation_Date,Max_datatarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataseguimento)
END AS [days_complete],

CASE WHEN Max_dataproxima_tarv>Max_dataproximaconsult
	THEN DATEDIFF(dd,Evaluation_Date,Max_dataproxima_tarv)
	ELSE DATEDIFF(dd,Evaluation_Date,Max_dataproximaconsult)
END AS [days_schedul]

FROM Sandbox.dbo.TX_CURR_JUN19

Go
